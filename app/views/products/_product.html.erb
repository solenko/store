
				<div class="catalog_item">
					<div><% if product.productimages.present? %><%= link_to image_tag(product.productimages.first.image_url(:catalog).to_s), product_path(product) %><% end %></div>
					<p><%= product.name %></p>
					<p class="size"><%= product.price %> грн</p>
				</div>



		