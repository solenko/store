<div class="fields" align="center">
        <%= f.select :category_id, nested_set_options(Category, @category) {|i| "#{'-' * i.level} #{i.name}" }, { :include_blank => true }%>
        <%= link_to_remove_fields "удалить", f %>
		<br/>
</div>